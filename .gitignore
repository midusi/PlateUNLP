# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local
.env.local
# Sin barra final: así también matchea cuando `.local` es un symlink
# (p. ej. un worktree apuntando a la base de datos del checkout principal)
/.local

# TanStack
node_modules
.nitro
.output
.tanstack

# Fumadocs
.source

# Claude Code: la config (.claude/launch.json, etc.) SÍ se versiona.
# Sólo se ignoran los worktrees temporales que crean los agentes.
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
