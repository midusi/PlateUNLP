import type { StepProps } from "@/interfaces/StepProps"

export function StepFeatureExtraction({ index, processInfo }: StepProps) {
    return (
        <>
            <h1> FEATURE EXTRACTION </h1>
            <p>{index}</p>
        </>
    )
}
